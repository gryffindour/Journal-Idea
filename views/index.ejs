<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Journal Idea ✨</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom scrollbar for a touch of modern feel (optional) */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #ffe6e6;
            border-radius: 10px;
        }
        ::-webkit-scrollbar-thumb {
            background: #958f8f;
            border-radius: 10px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #575454;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-indigo-50 to-purple-100 min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl w-full space-y-8 bg-white p-10 rounded-xl shadow-2xl transform hover:scale-105 transition-all duration-300">
        <div class="text-center">
            <h1 class="text-5xl font-extrabold text-gray-900 mb-4 tracking-tight leading-tight">Journal Idea</h1>
            <p class="mt-2 text-xl text-gray-600">Kelola ide jurnal kamu, supaya ga lupa!</p>
        </div>

        <a href="/add" class="mt-8 group relative w-full flex justify-center py-3 px-6 border border-transparent text-lg font-medium rounded-lg text-gray-800 bg-rose-200 hover:bg-rose-300 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-rose-100 transition-colors duration-200">
            ➕ Tambah Ide Jurnal
        </a>

        <% if (items.length > 0) { %>
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200 shadow-md rounded-lg overflow-hidden">
                    <thead class="bg-indigo-50">
                        <tr>
                            <th scope="col" class="px-6 py-3 text-left text-sm font-semibold text-gray-700 uppercase tracking-wider">Nama</th>
                            <th scope="col" class="px-6 py-3 text-left text-sm font-semibold text-gray-700 uppercase tracking-wider">Deskripsi</th>
                            <th scope="col" class="px-6 py-3 text-center text-sm font-semibold text-gray-700 uppercase tracking-wider">Aksi</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        <% items.forEach(item => { %>
                            <tr class="hover:bg-gray-50 transition-colors duration-150">
                                <td class="px-6 py-4 whitespace-nowrap text-lg font-medium text-gray-900"><%= item.name %></td>
                                <td class="px-6 py-4 whitespace-nowrap text-md text-gray-600 max-w-xs truncate"><%= item.description %></td>
                                <td class="px-6 py-4 whitespace-nowrap text-center text-sm font-medium">
                                    <div class="flex items-center justify-center space-x-4">
                                        <a href="/edit/<%= item._id %>" class="text-indigo-600 hover:text-indigo-900 font-bold transform hover:scale-110 transition-transform duration-200">
                                            ✏️ Edit
                                        </a>
                                        <form action="/items/<%= item._id %>?_method=DELETE" method="POST" class="inline-block">
                                            <button type="submit" onclick="return confirm('Yakin mau hapus item ini? Ini permanen lho! 😱')" class="text-red-600 hover:text-red-900 font-bold transform hover:scale-110 transition-transform duration-200">
                                                🗑️ Hapus
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <div class="text-center p-6 bg-gray-50 rounded-lg shadow-inner">
                <p class="text-xl text-gray-700">Ups, belum ada apapun di sini! Yuk, <a href="/add" class="text-indigo-600 hover:text-indigo-800 font-semibold underline">tambah idemu</a> sekarang! 👇</p>
            </div>
        <% } %>
    </div>
</body>
</html>